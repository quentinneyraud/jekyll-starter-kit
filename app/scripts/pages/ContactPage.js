import Page from './Page'

const dbg = debug('app:ContactPage')

export default class ContactPage extends Page {
  constructor () {
    super()
    dbg('Init ContactPage')
  }

  initializeElements () {
    super.initializeElements()
  }
}
